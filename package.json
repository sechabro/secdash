{
  "name": "secdash",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild js/index.js --bundle --outfile=js/bundle.js --minify --format=iife --keep-names --loader:.json=text",
    "watch": "esbuild js/index.js --bundle --outfile=js/bundle.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "chart.js": "^4.4.9",
    "echarts": "^5.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}